<div class="card-columns">
  <div class="card border-light mb-3 bg-dark" style="width: 23rem;">
    <h5 class="card-header">投稿者：<%= log.user.name %></h5>
    <p class="day">練習日：<%= log.practice_day %></p>
      <div class="card-body border-top border-light">
        <%= link_to "詳細", log_path(log.id), method: :get, class: "btn btn-primary" %>
        <% if user_signed_in? && log.user_id == current_user.id %>
          <%= link_to '編集', "/logs/#{log.id}/edit", method: :get, class: "btn btn-success" %></li>
          <%= link_to '削除', "/logs/#{log.id}", method: :delete, class: "btn btn-danger" , data: { confirm: '投稿を削除します。よろしいですか？' } %></li>
        <% end %>
        <p class="card-text">投稿日時：<%= log.created_at %></p>
        <div class="likes" id="likes_buttons_<%= log.id %>">
          <%= render partial: 'likes/like', locals: { log: log, likes: @likes } %>
        </div>
      </div>
  </div>
</div>