<% unless user_signed_in? %>
  <div class="main-info">
    <%= image_tag "tennis-image.jpg", size: "1440x395" %>
    <pre class="main-info__text">
      Tennis Notesは日々の練習を記録できるアプリです。
      練習の積み重ねを見える化する事で、モチベーションにも繋がります。
      日々の練習をより濃いものとし、ライバルに差を付けましょう！
    </pre>
  </div>
<% end %>

<% if user_signed_in? %>
  <div class="wrapper">
    <div class="main__content">
      <div class="main__content--cards">
        <% @log.each do |log| %>
          <%= render partial: 'card', locals: { log: log } %>
        <% end %>
        <%= paginate(@log) %>
      </div>
      <%= render 'layouts/sidebar' %>
    </div>
  </div>
<% else %>
  <div class="main">
    <div class="main__content">
      <div class="main__content--cards">
        <% @log.each do |log| %>
          <%= render partial: 'card', locals: { log: log } %>
        <% end %>
        <%= paginate(@log) %>
      </div>
      <%= image_tag "tennis-racket.jpg", width: 440 %>
    </div>
  </div>
<% end %>
